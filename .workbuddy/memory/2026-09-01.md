# 2026-09-01

- 按 docs/V1_PRODUCT_AND_TECHNICAL_PLAN.md（SSOT V1.0）完成 M0–M6 全量开发：React+TS+Vite+Tailwind 移动优先 PWA，工作区根目录即 app 根（GitHub→Vercel 零配置导入）。
- 已实现：画廊（按记录/按地点+场景筛选）、记录（多图压缩 2560/640+按住说话 webm→wav）、AI 确认页（本地推测预填+全字段可改+确认才入库）、记录/地点详情、找地点（自然语言→筛选，含中文数字解析「人均五十」「给四星」）、标签父→子两层、我的（导出 JSON/CSV+媒体清单）、单地点/清单分享快照、地图总览（高德 JS API 适配+未配置纸感示意底图降级）、IndexedDB+outbox 幂等同步引擎、supabase/migrations/0001_init.sql（8 表+RLS+2 bucket）。
- 降级设计：全部外部能力（Supabase/Google/腾讯 ASR/OpenRouter/DeepSeek/OpenCode/高德）未配置时明确提示，AI 降级为本地推测（中文数字可解析），地图降级为示意底图。
- dev 下 /api 404 需按 content-type/404 判定未部署（已处理）；Vercel 部署后自动接通真实函数。
- 演示数据 6 地点带海口市地区标签（否则地区筛选 0 结果）；demo_seeded meta 保证清除后不复活。
- 已验证：tsc 通过、vite build+PWA SW 生成、agent-browser 实测画廊/找地点/记录→AI确认/分享清单/地图降级全链路。
- 未做（待 Key）：真登录、ASR/大模型真实调用、高德真实瓦片、真实 Supabase 写入、Vercel 部署、真实照片压缩验收。未 commit（等用户确认）。

## 第二轮 QA（同日继续）
- 实测通过：记录详情（相册/封面切换）、地点详情时间线、按地点视图、标签页两层结构、单记录删除、生产构建 preview（4175，PWA SW 200）。
- 修复 1 个 P0：草稿经 sessionStorage 序列化导致 Blob→{}，带照片确认页崩溃。改用内存模块 src/lib/draft.ts。
- 改进：dev 下 /api 404 判定未部署；中文数字解析 cnToNumber（人均五十/给四星）；演示数据补海口市地区标签；标签名 token 匹配（「拍照」命中「拍照打卡」）。
- 压缩实测：3000px/96KB 原图 → 展示+缩略共 73KB。
- 经验已沉淀到 ~/.workbuddy/skills/place-journal-qa/SKILL.md。
- 4173 端口被其他进程占用（* :4173，未动它），preview 换 4175。

## 第三轮 QA + 功能补全（23:45）
- 实测通过：导出 JSON（schema v1，6地点/6记录/26标签/12媒体清单）、CSV（BOM+转义正确）、单地点分享页（/s/p/ 渲染正常）、撤销分享全链路、清除演示数据（0/0）、空状态文案、演示数据重灌、手机 390px 视口（画廊/找地点/我的均无横向滚动）。
- 补全功能缺口：分享撤销原来只有数据层 repo.revokeShare + outbox revoke_share + 读端失效提示，缺发起端 UI。Mine 页新增「🔗 我的分享」区块（复制链接/撤销，confirm 确认，撤销后区块消失、链接立即失效）。
- 观察项（未改）：找地点页悬浮「记录」按钮与列表选择圆圈视觉重叠，可滚动避开。
- agent-browser 视口命令语法：`agent-browser set viewport 390 844`（不是裸 viewport）。
- 最终 build 通过（PWA precache 7 entries / 488KB）。

## 演示图换免费通用图（00:11，9-2 凌晨）
- 用户反馈封面「示意图」文字被卡片裁切。已把 demo.ts 的 SVG 占位图整体换成 picsum.photos seed 图（960/720，无需 Key），第二张照片用 -extra-N seed。
- 重灌演示数据后实测：画廊 6/6 加载、详情页 2 图、分享快照照片均正常，遮挡消失。
- 最终 build 通过。代码仍未提交。

## 按共享 Supabase 规范 V1.1 收口（9-2 凌晨）
- 用户提供规范文档（共享项目+独立 Schema）。已按规范改造：
- 迁移 SQL 重写：8 表从 public.place_* 移入独立 Schema `habit_tracker`（去前缀）；owner_user_id references auth.users；revision 乐观锁列（places/entries/tags/tag_dimensions）；updated_at 触发器；三道门 SQL（GRANT usage+表权限，anon 仅分享快照只读）；entry_tags 归属一致性用触发器校验（PG CHECK 不允许子查询）；Realtime publication 5 表；bucket 改名 habit-tracker-media-*；头部含设计问答与「提交共享平台仓库」标注。
- 客户端：supabase.ts 导出 DB_SCHEMA + table() helper（schema('habit_tracker')）；env.ts 改 VITE_SUPABASE_PUBLISHABLE_KEY（兼容旧 ANON_KEY）；sync.ts 全部查询走 schema、owner_user_id、revision 自增（idb savePlace/saveEntry）、entry_tags 与 share_items 改「先增后删」差量同步；分享页旧表名修复。
- README 更新数据库接入章节（Migration 归属共享平台仓库、Expose 步骤、冲突规则）。
- tsc + build 通过（precache 7 entries）。规范要点：工具仓库不维护生产 Migration 历史；Expose 只能在 Dashboard 配置。
