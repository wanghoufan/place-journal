# 2026-09-05

## 复测补线修复（接续 2026-09-04 深夜二次收工）

- 用户跳过手机复测，授权桌面自动化代测（5173 + 既有 /tmp/chrome-cdp-profile 登录态复用，真实 Chrome CDP 9334）。
- **发现 RQA-V（6705e26）实为两处半成品**：
  1. `deleteEntry` 级联从未接线——idb/sync 管道（revokeShare 按 slug、ShareItem.entryId、delete_place op）都在，但没有调用方；
  2. EntryDetail 确认弹窗是死代码——没有任何 `setConfirmDel(true)` 调用，普通视图删除按钮直接执行（上轮「弹窗已修」验收不实）。
- **修复**（未 commit）：`src/lib/idb.ts` deleteEntry 内联级联（撤销分享 + 空地点清理 + delete_place op）；`src/pages/EntryDetail.tsx:188` 改 `setConfirmDel(true)`。
- **实测全过**：弹窗四要素 + 取消路径；删后匿名 RPC `public_share_read` 返回 null；空地点云端级联清除（delete_place → places 行删 → FK cascade 带走 entries）；多记录地点删单条不触发地点删除（B 场景）。
- **复活风险实锤（§0.2-10）**：pullRemote 全量补插，手动拉取把已删记录拉回；云端现存 26 entries/27 places，大量历轮 QA 重复播种 + 验收残留；on-load autoSync 竞态（cloudState 非 idle 跳过）掩盖日常复活。修复方案待用户拍板（delete_entry op + RLS DELETE policy 检查 + 僵尸清理需逐项确认）。
- 测试数据全部清理（测试地点/记录/分享云端+本地均删）；QA Chrome profile 本地因手动 pull 已混入云端僵尸数据（仅该 profile，不影响手机）。
- **收口（用户拍板：1 可以 / 2 你定）**：
  - 新增 `delete_entry` outbox op（复用 0001 owner DELETE policy，无 Schema 变更），多记录地点删单条也删云端行——复活根因根治；场景 C 实测 PASS。
  - 云端大扫除：8 表业务行全清（26/27 全为历轮 QA 重复播种+验收残留），两桶递归删 6 个测试对象；QA profile 本地 wipeLocal 重置。
  - commit `9f0eeb7` push → Vercel 自动部署；8081 deploy.sh 重建（镜像 f00f7dec9fe6，healthz 200）；两端 bundle 均含 delete_entry 实证，三端同码。
  - 遗留观察：on-load autoSync 竞态未修（Realtime 订阅成功会补拉，可接受）；用户真实设备旧记录云端行已清，日后编辑旧记录若冲突走 Mine 页裁决 UI。
  - 仍待用户示下：§0.2-11 的 /tmp QA profile、Downloads 测试分享图、用户主浏览器本地历史验收标签、.env.vercel.local；高德 Key 联调。
- **第三轮（用户反馈评分筛选疑问，选方案 A）**：
  - 澄清：筛选结果是动态的（实测 UI 改评分 5→1 后 5 星档正确归零、改回后恢复）；chips 固定三档是预设设计；评分是记录数字字段，不属于标签维度体系（Mine 标签与维度里没有属正常）。
  - 排查插曲：agent-browser click ref 误点/eval textContent 匹配不到 SVG 星标按钮（Stars 组件是 aria-label button），需用 `[aria-label="5 星"]` 定位编辑表单星标。
  - 方案 A 落地：Find 评分档扩为 5 档（1/2/3/4/5 星，`{[1,2,3,4,5].map}`）；实测五档计数与数据全吻合（6/6/6/5/1）。
  - **收口（用户确认「提交」）**：commit `6227d56` push → Vercel 自动部署；8081 deploy.sh 重建（镜像 606efa9037af，healthz 200）。bundle 验证注意：档位文案是模板拼接（`${n} 星以上`），grep 要用 `星以上` 或 `\[1,2,3,4,5\]\.map`，直接搜「1 星以上」必得 0。两端 bundle 已实证含 5 档数组。
- **第四轮（用户新需求：编辑记录时可现场建标签）**：
  - EntryDetail 编辑表单标签区加「＋ 新标签」入口 → Sheet：名称输入 + 维度 chips（默认 custom 维度，无则第一维度）→ 创建即自动勾选；同名标签提示并「选用现有标签」防重复。
  - 测试中发现连带漏洞：demo 记录被显式编辑保存仍会上云（saveEdit 入队 upsert_entry，直推）。修补：sync.ts upsert_entry 分支遇 demo 行直接丢弃 op（与 sweep 同口径）。实测：demo 显式保存 → op 1→0 丢弃、云端保持 0。
  - 实测全过：创建→自动勾选→保存→entry.tagIds 持久化；云端同步（tag/entry/entry_tags 推送正确——测后已把云端 QA 数据再清零）；同名复用不产生重复；TagsPage 新维度组下可见。
  - 改动文件：`src/pages/EntryDetail.tsx`（功能）+ `src/lib/sync.ts`（demo-skip）。build 通过。
  - **收口（用户确认「提交」）**：commit `08bae21` push → Vercel 自动部署；8081 deploy.sh 重建（镜像 997aea868460，healthz 200），两端 bundle 实证含「新标签」入口。
- **收工（用户宣布开发暂停）**：HANDOFF.md 标题与 §0 快照更新至 2026-09-05（四批上线汇总 + 收工状态行 + §0.2 待办 11/12/13 更新 + §0.3 新增规则 10 浏览器自动化坑），docs 快照 `6dafc82` 已推。当日 HEAD 链：9f0eeb7 → 00c115d → 6227d56 → 08bae21 → 6dafc82（全在 origin/master）。
- **洁癖收尾（用户 @neat-freak）**：完整路径审计。发现 14:10 完成的双智能体复盘 Run（Orca/Muse Spark）在工作区留有未提交改动（AGENTS/HANDOFF/README 三根文档更新至 09-05 现役，内容核对准确）+ `coordination/`（STATE.md：run_1b691ae4e183 CONVERGED）+ `docs/review/`（Day1 反推与差距分析 V1.0/V1.1 共 8 文件）。小修：根 HANDOFF「§0 现役 09-04」→09-05、「§0.5」→§0.2-5 死引用；docs/handoff §1 必读补 09-05 日志；§0.2-11 tmp 清单核实现（rqa-profile 已不存在、chrome-cdp-profile 仍运行）；补建 09-04 日志断档占位（指针型）。待用户确认项：未提交文档批次是否 commit、coordination/ + docs/review/ 去留、/tmp 清理。
- **第二轮（用户问 demo 数据占云 + 评分筛选）**：
  - 查实 demo 数据泄漏通道 = sweepDirtyRows 捞 sync='local' 行（demo 播种行恰为 local）；修复：demo entry 永不上云、demo place 仅被真实记录引用时放行。实测：本地 6 条 demo、sync 两次后云端 0/0、outbox 0。
  - Find 页新增评分筛选（评分组 chips 3/4/5 星以上，单选可取消，按地点最高分记录过滤）：6→5（≥4 星滤掉老爸茶 3 星）、5 星→1（绿野书屋）核对吻合。
  - 两项改动 tsc+build 通过、实测通过。**收口（用户确认「提交」）**：commit `00c115d` push → Vercel 自动部署；8081 deploy.sh 重建（镜像 4ec462f7d672，healthz 200），两端 bundle 实证含评分筛选文案，三端同码；HANDOFF §0.1 补行 + docs 快照 `4389072` 已推。
