# 真机 QA 报告丨Vercel 上线生产验收丨2026-09-04

日期：2026-09-04（UTC+8 傍晚）
主测：**https://place-journal-xi.vercel.app**（Vercel 生产）· 真实 Chrome 独立 profile（`--user-data-dir=/tmp/rqa-profile-ef894e7a`，调试口 9336，仅做导航/读数，页内交互走 Computer Use 真实点击）· 桌面 1280×900 + CDP 移动视口模拟 390×844
附测：http://localhost:8081（curl 冒烟）
登录：用户选择**跳过亲手登录**（见 B1），本轮=未登录本地态 + 生产服务端（ASR/AI 已配置）实测。

## QA Result

### 总体结果：**BLOCKED**（登录缺席：云同步/真转写/匿名外发不可验；本地全链与视觉走查通过，无 P0/P1）

### 测试统计：Total 30 / Passed 22 / Failed 3 / Blocked 5

- Passed 22：未登录首页、我的页、记录三态、新地点表单、AI 整理（速度/质量）、标签点选、保存入库、刷新持久、编辑 5 星、二次刷新、分享面板、分享图下载、同窗分享渲染、分享撤销、3 主题+持久、Find、Tags、移动 390（首页/记录）、照片上传、连点防重、8081 冒烟、ai-organize 400 拒空。
- Failed 3：RQA-V-01（P3 删除无确认）、RQA-V-02（P2 删记录后分享不联动）、RQA-V-03（P3 空地点残留）。
- Blocked 5：B1 OAuth 亲验回跳、B2 登录后云同步/outbox、B3 真人语音→转写回填、B4 超短按 toast 真机、B5 异 profile 匿名外发。

### Bug 列表

#### RQA-V-01：删除记录无二次确认（与“撤销分享有确认”不一致）
- Severity：P3
- Environment：Chrome 桌面；详情页 `/entry/:id`；未登录
- Steps：1. 建 QA 记录并保存进详情页；2. 点「删除」。
- Expected：破坏性且不可恢复的删除应有一次确认（同页「撤销分享」就有原生确认框）。
- Actual：一次点击直接删除并跳回首页，无任何确认。源码佐证 `src/pages/EntryDetail.tsx:185`：`onClick={async () => { await repo.deleteEntry(entry.id); nav('/', { replace: true }) }}`。
- Visual Evidence：`evidence-rqa-vercel/12-删除后首页.png`（已回到首页，记录消失）。
- Reproducibility：Always
- User Impact：误触即丢数据；与撤销分享的确认体验不一致。

#### RQA-V-02：删除记录后，其分享快照不联动处理，链接与列表残留
- Severity：P2（隐私预期差）
- Environment：Chrome 桌面；我的页；未登录
- Steps：1. 建记录→分享（得 slug 链接）；2. 删该记录；3. 看我的页。
- Expected：用户删记录时，默认也失效/提示其分享链接（或明确告知链接仍有效）。
- Actual：`我的分享（1）` 仍在，复制链接可用（同窗实测渲染完整字段），`地点/记录 7/6`。需手动进「我的分享→撤销」才归零（撤销本身有确认框，体验好）。
- Visual Evidence：`evidence-rqa-vercel/10-分享按钮点击后.png`（面板含可用链接）、`13-撤销分享确认框.png`。
- Reproducibility：Always
- User Impact：用户以为“删了就没了”，旧链接仍可打开快照（字段白名单内），属隐私预期落差。

#### RQA-V-03：删除记录后空地点残留（7 地点/6 记录），无产品删除入口
- Severity：P3（已知边界，本轮复现；残留仅在待丢弃的 RQA 隔离 profile，未污染云端/用户数据）
- Environment：Chrome 桌面；我的页/按地点；未登录
- Steps：1. 新地点建记录；2. 删该记录；3. 我的页看计数 + 首页「按地点」。
- Expected：空地点自动清理或提供删除入口。
- Actual：`地点 / 记录 7 / 6`；按地点仅 6 个有记录地点（空地点不展示但计数残留）。另删除后「最近：QA测-…」曾残留一行，撤销分享后消失。
- Visual Evidence：`evidence-rqa-vercel/14-主题票根-我的页.png`（可见 7/6）。
- Reproducibility：Always
- User Impact：计数与实际对不上；空地点越积越多只能留着。

### 白名单对照（本轮结论）
- ① 分享创建后需下次同步：未登录无法验证同步时机；本地快照链接同窗可用（`/s/p/94wc8xl63vuiyvka` 字段完整：店名/预算/公开理由/咖啡标签/三主题/地图外链）。
- ② owner 封面空白：本轮记录无照片，n/a。
- ③ 同步错误重置：未登录无同步，未涉及。
- ④ 历史验收标签：Tags 页 26 个维度标签可见范围内**未见「验收T3/T7」**（地区/类型/场景/人群分组正常，计数如咖啡茶饮·2 次使用）。
- ⑤ 冷启动：两次 AI 整理均 **<5s 出结果且无降级提示条**（deepseek-v4-flash 实测快），冷启动+12s 降级提示**本轮未触发、无从评价**。
- ⑥ 本地推测模板化：本轮为真实 AI（摘要「环境不错，咖啡香，适合一人放空」非模板，预算 40 解析精准，评分预填 4 星），附带小瑕疵：AI 候选里冒出无关城市「儋州市」（观察项，非 bug）。

### 旅程记录（证据链）
- 旅程 1：未登录首页渲染正常（暖纸、2 列卡片、banner「已连接云端·尚未登录，去「我的」用 Google 登录」）→ 我的页（云端已配置·未登录/数据 6/6/照片 12/1.7MB/ASR 已配置/AI 已配置/地图未配置）→ 点「使用 Google 登录」同 tab 秒跳 accounts.google.com（`client_id=3765…`、`redirect_uri=…supabase.co/auth/v1/callback`、`scope=email+profile` 正确）→ **用户选跳过登录**，后退回生产站继续。证据 01/02/04。
- 旅程 2（本地态）：记录页三态按钮验证（未选地点禁用文案「先选择地点，再交给 AI 整理 →」）→ 新地点 QA测-生产咖啡馆+感受 → AI 确认 <5s（4 星/¥40/摘要/咖啡待选/新场景「下午茶」）→ 入库进详情。麦克风权限弹窗**真实捕获**：地址栏「使用麦克风？」+ 浮层（设备 USB ENC Audio Device、三选项：访问该网站时允许/仅这次/一律不允许），文案中性不吓人，不遮挡录音钮；以「关闭」中性 dismissal，未改授权状态。合成 250ms 短按未见 toast（权限弹窗先行，真人短按待补）。证据 05/06/20/22。
- 旅程 3（本地闭环）：刷新数据在 → 编辑改 5 星保存 → 再刷新保持（截图 5 星）→ 分享面板（本地快照说明清晰）→ **真实下载落盘** `打卡分享·QA测-生产咖啡馆.jpg`（JPEG 1080×900，53KB，文件名含店名）→ 同窗开分享链接字段全 → 撤销分享（有确认）归零 → 删连点测试记录 → entries 6/6。证据 07–13。
- 旅程 4（移动 390 模拟）：首页双列不断裂（芯片横滑为设计）、记录页单列全控件无重叠截断；键盘遮挡/权限样式/Toast 位置需真机补。证据 18/19。
- 8 页走查：首页/详情/记录/AI 确认/相册（首页按记录即画廊）/找地点（分组筛选+6 结果+选择）/我的/分享快照（面板+单页）+ Tags 页均无错位重叠截断；FAB 与卡片轻叠为固有设计。证据 14–17。
- 三主题：暖纸→票根→竹青即时生效且协调，刷新后 `data-theme=warm`/`localStorage=warm` 保持。证据 14/15。
- 探索：空提交被禁用文案拦截；连点「确认保存」150ms 双击仅入库 1 条（IndexedDB entries 7→删后 6，无重复）；`ai-organize` 空包 400。
- 8081 冒烟：`/` `/find` `/mine` `/record` `/tags` 全 200，`/healthz` ok。

### 体验评价
- OAuth 跳转顺滑度：跳转本身干脆（同 tab、无白屏），回调参数正确；**回跳/登录态未验（用户跳过）**，不打分。
- 录音等待期：本轮 AI <5s 且有确认页即时转场，无焦虑感；但 12s 降级提示条未触发，冷启动首跳体验仍是盲区。
- 移动视口：布局不断裂，记录单列可用；真机键盘/热区/Toast 待补。
- 三主题：三套配色各自协调（票根暖赭、竹青豆绿），切换零闪烁，持久化可靠。

### Happy Path 结论 + 修复优先级
本地 Happy Path（记录→AI→保存→编辑→分享→下载→撤销→删除）**全通**。云 Happy Path（登录→同步→外发）**因跳过登录未验**。
1. RQA-V-02（P2）：删记录时联动提示/失效其分享（隐私预期）。
2. RQA-V-01（P3）：删除加一次确认，与撤销分享对齐。
3. RQA-V-03（P3）：空地点自动清理或给删除入口（含计数口径）。
4. 下轮补：亲手 OAuth（含回跳/已连接云端）、登录态云同步、真人语音转写、冷启动降级条、异 profile 匿名外发、真机 390 键盘。

## 数据安全
- 本轮**零云端写入**：全程未登录，所有 QA 数据（QA测-生产咖啡馆、连点记录、分享 slug `94wc8xl63vuiyvka`）只在一次性 RQA profile 本地；结束时记录删光（entries 6/6）、分享撤销归零；空地点残留随 `/tmp/rqa-profile-ef894e7a` 丢弃而消失。真实数据（绿野书屋/海边小酒馆等）只读未动。
- 截图无密钥/token；Google 页未输入凭据；感受文本均为 QA 虚构。
- 落盘文件：`~/Downloads/打卡分享·QA测-生产咖啡馆.jpg`（53KB，可删）；证据 22 张在 `docs/qa/evidence-rqa-vercel/`。

## 结束自检
- 主页正常 ✓ Happy Path（本地）✓ 核心交互 ✓ 登录/Session ✗（用户跳过→BLOCKED，主因）— 按规则判 BLOCKED，无 P0/P1。
