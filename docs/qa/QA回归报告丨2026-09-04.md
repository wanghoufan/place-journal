# 个人打卡小工具（habit_tracker）QA 回归报告

- 测试日期：2026-09-04（Asia/Shanghai）
- 被测地址：`http://localhost:8081`（Mac mini 自托管生产版）
- 基线：`docs/qa/QA-BASELINE丨V0.2.md`
- 浏览器：真实 Google Chrome；R5 使用 Chrome 无痕窗口；R3 已获得 localhost 麦克风权限
- 数据边界：仅尝试使用 `QA-` 前缀测试数据；未删除或修改绿野书屋、西海岸日落咖啡、验收测试地点及既有分享数据
- 测试图片：`/private/tmp/qa-habit-photo.jpg`，66,254 bytes，页面显示压缩后约 88 KB

## 结论

**QA_FAIL**

R2（AI 整理）、R3（语音转写）、R5（匿名分享）失败；R1 因 Google OAuth 回调错误且无法完成保存/首轮同步而失败。按本轮约定，任一 R 项失败即整体 FAIL。

## 今日修复回归

| 项目 | 结果 | 证据 |
|---|---|---|
| R1 带照片新记录首轮同步 | **FAIL** | 真实 Chrome 中照片选择成功：记录页显示“1 张 · 压缩后约 88 KB · 第一张为封面”。但点击「我的」→「使用 Google 登录」后，页面跳转到另一台本地服务的 OAuth 回调，回到 `localhost:8081` 仍显示“已配置 · 未登录”；因此无法保存记录、观察同步图标或验证 media/entry 顺序。未观察到可判定首轮成功的写入。 |
| R2 AI 整理端到端 | **FAIL** | 使用指定原文“今天去了海边咖啡馆看书很放松人均45”，AiConfirm 能显示原文，但等待约 5.5 秒后仍显示「整理中…」且按钮 disabled；评分、预算、人均 45、摘要、标签建议均为空，未能确认入库。R1 的测试文字也出现同样卡住。 |
| R3 语音转写端到端 | **FAIL** | 真实 Chrome localhost 麦克风权限弹窗出现并已允许；页面进入“正在录音…松开结束”，但当前桌面控制面无法产生可验证的松开事件，且没有可验证的口述音频输入；刷新后无转写文本出现。未将“录音状态出现”误记为转写通过。 |
| R4 找地点标签去重 | **PASS** | 找地点筛选条显示 6 个 chip，各名称只出现一次；点击「拍照打卡」后显示「找到 2 个私藏地点」，结果为西海岸日落咖啡、海口骑楼老街，筛选正常。 |
| R5 匿名分享字段完整 | **FAIL** | Chrome 无痕窗口访问 `/s/a3r3ppfnenp3dx4b`，页面显示「页面不存在」，无法显示店名/区域/评分/标签/封面图；因此字段完整性与公开桶匿名可读均未通过。 |

## 基线回归

### B1 编辑功能 6 项

**FAIL（部分只读通过，写入项未通过）**。既有「绿野书屋」详情页可见「编辑」按钮；编辑表单能回填日期 `2026-09-02`、人均 `35`、私密感受、公开推荐理由及标签；点击「取消」返回详情且数据显示不变（对应基线 A1/A5 的只读部分 PASS）。

但本轮禁止修改既有真实数据，且 QA 新记录无法因 R2 卡住而保存；未执行改名称/评分/笔记/加删标签/隐私开关的持久化验证。实际编辑表单未发现名称输入或隐私开关控件。未能验证刷新持久化、revision +1、云朵「已同步」或 `rest/v1/entries` 回执。

### B2 标签回归 T1–T8

**FAIL（页面可读，登录同步链路阻断）**。

- 标签与维度页可打开，父→子结构可见，例如「咖啡茶饮」下的「咖啡」、「美食」下的「海南菜」等。
- T1 FAIL：我的页明确显示「已配置 · 未登录」，上次同步为「—」。
- T2–T8：因无法登录且无法建立 QA 记录，未能完成删除引用清理、重命名联动、父链自动上云、快速打/删后的 outbox 与刷新一致性验证；不能凭页面静态标签宣布通过。

### B3 核心工作流：新建→编辑→分享→删除分享

**FAIL（阻断）**。新建流程可进入记录页并完成照片本地选择，但 AI 整理卡在「整理中…」，未能保存 QA 记录；因此后续编辑、生成分享、打开分享、删除分享均无法执行。指定现有分享 slug 在生产版返回「页面不存在」。

### B4 同步状态机：断网→恢复网络→自动补同步

**FAIL（阻断）**。我的页为「已配置 · 未登录」，没有可验证的 outbox/同步状态机入口；未进行断网写入或恢复补同步，以避免把本地未登录状态当作云同步证据。

### B5 滚动手势 K1

**FAIL（基线未全量闭环）**，但已完成的页面结果如下：

- D1 画廊：PASS。真实页面可从顶部滚到底部再回到顶部，卡片和底部导航正常显示。
- D2 记录详情：PASS。两张照片横向并列展示；详情页可上下滚动，未观察到横向照片区域劫持纵向滚动。
- D3 地点/设置：PASS（可见页面）。找地点、标签页、我的页打开正常；标签页可上下滚动，设置页内容可见。
- D4 分享页：FAIL。指定分享页为「页面不存在」，无法进行长内容滚动验证。
- D5 键盘编辑：⏳ 待验证。没有可安全修改的 QA 记录，且新建记录未保存。

## 网络请求关键行

以下为本轮可获得的关键证据；未把静态 bundle 中的字符串当作运行时请求：

```text
GET http://localhost:8081/s/a3r3ppfnenp3dx4b
HTTP/1.1 200 OK
content-type: text/html; charset=utf-8
页面渲染结果：页面不存在
```

生产前端 bundle 可读到的接口/桶名包括：`/api/transcribe`、`/api/ai-organize`、`habit-tracker-media-private`、`habit-tracker-media-share`。本轮没有获得成功保存回执，因此没有观察到可用于 PASS 的：

- `/storage/v1/object/habit-tracker-media-private/...`
- `/storage/v1/object/habit-tracker-media-share/...`
- `rest/v1/entries`、`tags`、`entry_tags` 成功写入及 revision 回执

R2 卡住期间未取得可导出的 Network 面板响应状态；仅能确认页面状态与接口命名，不能据此推断后端成功或失败。

## 证据截图

本轮浏览器控制会话已内联留存以下截图证据：

1. R2 AiConfirm 页面：原文显示、评分/预算/摘要为空、按钮「整理中…」disabled。
2. R4 找地点页：筛选条无重复 chip，点击「拍照打卡」后结果为 2 个地点。
3. R5 无痕窗口：`/s/a3r3ppfnenp3dx4b` 显示「页面不存在」。
4. B5：画廊顶部/底部、记录详情页、标签页滚动截图。

受当前桌面截图权限限制，截图只能作为本轮浏览器控制会话的内联证据，未能导出为本地 PNG 路径；未伪造截图文件路径。

## 未覆盖与恢复条件

1. 先修正生产版 Google OAuth redirect，使登录后仍回到 `localhost:8081` 并显示已登录；不要在本轮 QA 中代输密码或复用/复制令牌。
2. 修复或定位 `/api/ai-organize` 长时间停留「整理中…」的问题后，重跑 R1/R2，并记录真实 Network 状态码与成功回执。
3. 用真实 Chrome 完成人工按住/松开录音并说出指定句子，重跑 R3。
4. 生成一个 `QA-` 分享或修复现有 slug，使用无痕窗口验证白名单字段和 `habit-tracker-media-share` 公共封面加载。
5. 登录后再执行 B1–B4 的写入、刷新回读、离线补同步和 T1–T8；删除动作仍只针对 QA- 数据。

