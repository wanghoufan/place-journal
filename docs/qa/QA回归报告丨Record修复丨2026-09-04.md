# QA回归报告丨Record修复丨2026-09-04

- 被测版本：本地 `35538b8`（含 `src/pages/Record.tsx` 三项修复，`git status` clean；领先 `origin/master` 1 commit 未 push）
- 主测：`http://localhost:5173`（vite dev，HTTP 200）；附测 `http://localhost:8081` 冒烟（HTTP 200；镜像早于修复，仅冒烟）
- 浏览器：独立 profile `/tmp/qa-profile-20260904r` + `--use-fake-ui-for-media-stream --use-fake-device-for-media-stream`，CDP 自动化（零依赖 Node 22 脚本 `/tmp/qa-run.mjs` / `qa-run2.mjs` / `qa-run3.mjs`），SW 已 wipe（`wiped:0/0`，无旧 bundle 干扰）
- 结论：`QA_PASS_WITH_BLOCKED`（P0 3/3 通过；P1 5/6 通过，1 BLOCKED；观察项 1 个 P3）

## 测试统计：Total 11 / Passed 9 / Failed 0 / Blocked 2

| ID | 内容 | 结果 | 证据 |
|---|---|---|---|
| R1 | 封面切换（2 张→设为封面→顺序交换） | PASS | `R1-01-two-photos.png` `R1-02-after-setcover.png` `R1-03-order-proof.png`；`srcs` 交换 `f653…↔9f79…`，`R1_swapped=true` |
| R2 | 录音降级（fake device 短按友好提示） | PASS | `R2-record.png`；`isSecureContext=true`，`supported=true`，`gum-ok tracks=1`，短按 note=`好像没录到声音…` |
| R2-HTTP | HTTP 非安全上下文提示文案 | BLOCKED | 桌面 localhost恒为安全上下文，无法触发；交手机真机（`192.168.31.60:8081`） |
| R3 | AI 按钮三态（缺地点/缺内容/可点） | PASS | `R3-01-initial.png` `R3-02-after-place-text.png`；初始`先选择地点…`disabled→选地点后`添加照片或说说感受`disabled→有内容后`交给 AI 整理 →`enabled |
| B1 | 保存全链（地点+文字→confirm→入库→详情） | PASS | `B1-01-confirm.png` `B1-02-after-save.png`；`/confirm` 本地推测降级（`AI 未配置…本地推测预填`），预算`¥50`正确解析，`/entry/803845a5-…` 详情含验收文本 |
| B2 | 标签管理页（改名/删除入口+使用计数） | PASS | `B2-tags.png`；`海南省·6次使用`/`未使用`等计数渲染正常 |
| B3 | Find 标签 toggle（选中/取消/交集） | PASS | `B3-find.png` `B3-toggle-proof.png`；`海口市` init→click1选中`✓+chip-active`→click2取消→click3再选中（`b819931` 已修得证） |
| B4 | 分享链路（sheet→快照页字段完整） | PASS | `B4-01-share-sheet.png` `B4-02-snapshot.png`；`/s/p/4dolpwxzkm4dwblr` 含店名/人均/标签/`noUndefined=true` |
| B5 | 主题切换+持久化 | PASS | `B5-mine-theme.png`；`ticket` 写入 localStorage 并 reload 保持；测后已重置回 `warm` |
| B6 | 云同步（outbox→上云） | BLOCKED | 未登录（红线禁代输凭据）；`outbox.count=1` 证明入队正常，待真实登录态 |
| SMOKE | 5173 五路由 + 8081 | PASS | `B-smoke-*.png`；五路由均非白屏；8081 `HTTP 200 len=654` |

## Failed Cases
无。

## BLOCKED Cases
1. R2-HTTP：`window.isSecureContext` 在桌面 localhost 恒 true，`浏览器要求 HTTPS…` 文案走不到；需手机 `http://192.168.31.60:8081`（HTTP 非安全上下文）真机验证。
2. B6：无 Google 登录态（OAuth 须用户亲自完成），云端推送/回读未测；本地 outbox 入队已证（`count=1`，entry `803845a5-…`）。

## 观察项（非 FAIL）
- OBS-1（P3，先存）：Gallery 有 React `validateDOMNesting: <button> cannot appear as descendant of <button>`（`src/components/ui.tsx` Stars 嵌套于 EntryCard button，`src/pages/Gallery.tsx:217`）。功能无影响，建议后续顺手解嵌套。
- 说明：dev 下 `404 /api/transcribe` `/api/ai-organize` 为预期降级（`src/lib/organize.ts:19,41` 明确把 404/text-html 判 `not_configured` 走本地推测），B1 实测降级文案与功能均正常，不计 FAIL。
- 说明：R3 初始态（无地点+无内容）按钮为`先选择地点…`（地点优先），与任务矩阵①字样不同但语义正确（disabled+明示缺项），计 PASS。
- 测试数据（按“验收”字样+测后保留约定）：entry `803845a5-2f40-4652-b8eb-0cd8eef3975b`（`QA验收保存链…`）+ 本地分享 `/s/p/4dolpwxzkm4dwblr`；outbox 残留 1 条待登录后自愈。

## 复现（任一 Case 单独重跑）
1. `PROFILE=/tmp/qa-profile-x; "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --no-sandbox --disable-dev-shm-usage --window-size=390,844 --remote-debugging-port=9336 --user-data-dir="$PROFILE" --use-fake-ui-for-media-stream --use-fake-device-for-media-stream about:blank &`
2. `/Users/zzymima0000/.workbuddy/binaries/node/versions/22.22.2-2/bin/node /tmp/qa-run.mjs`（冒烟+R1/R2/R3/B2/B3/B5）
3. 同上 `node /tmp/qa-run2.mjs`（R1 顺序实证+B3 精确 toggle+B1 保存链）`, node /tmp/qa-run3.mjs`（B4 分享+主题持久化）
4. 退出走 SIGTERM（`kill <pid>`），禁用 `pkill -9`（会挂死 profile 的 IndexedDB）。
