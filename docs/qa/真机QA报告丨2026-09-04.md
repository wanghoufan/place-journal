# 真机 QA 报告｜个人打卡小工具（habit_tracker）

日期：2026-09-04  
主测：http://localhost:5173  · Chrome 独立 QA 会话  · 390×844；另做桌面宽视口首页检查  
附测：http://localhost:8081  · Chrome  · 首页/详情/三 tab 冒烟

## QA Result

### 总体结果：PASS（含待验证项）

登录与核心记录链路已在真实 Chrome 中完成：Google 会话复用、地点选择、AI 确认、保存、详情刷新、编辑、分享长图、分享链接和 Find 回顾均可用。照片文件选择器未能在当前 Computer Use 会话中弹出，录音按住/松开无法由当前输入接口模拟，因此照片封面切换、真实录音转写标记为 ⏳ 待验证；5173 当前 AI/ASR 配置显示为未部署，实际使用本地降级整理。

### 测试统计：Total 18 / Passed 14 / Failed 0 / Blocked 0 / Pending 4

统计口径：Pending 包括真实照片上传与封面切换、真实录音转写、桌面+移动 8 页完整截图归档、无痕分享与下载文件落盘核验。

## Bug 列表

本轮未发现可稳定复现的 P0/P1 产品缺陷。

### 环境观察项（不计 FAIL）

#### RQA-OBS-01：5173 开发环境未启用 ASR/AI 服务

- Severity：P2（环境/配置观察）
- Environment：Chrome；Record → AI Confirm；已登录
- Steps：1. 打开 Record；2. 选择 `QA测-真机咖啡馆` 并输入 QA 文案；3. 点击“交给 AI 整理 →”。
- Expected：调用已配置的 AI 服务并明确显示处理结果。
- Actual：确认页显示“AI 未配置（需在部署环境填入大模型 Key）”，随后按本地推测预填；字段完整，仍可确认保存。
- Visual Evidence：本轮移动确认页截图在会话中已捕获；当前运行环境未提供将截图二进制写入 `docs/qa/evidence-rqa/` 的能力，未伪造截图文件。
- Reproducibility：Always（当前 5173 配置）
- User Impact：用户仍能完成记录，但无法获得真实模型整理质量；需在 5173 配置服务后复测。

## 走查结论

- 首页：桌面容器居中、暖纸主题协调；移动首页可打开，列表滚动正常。
- 详情：移动详情字段、操作按钮和长文本布局自然，无重叠/截断；QA 记录刷新后仍存在。
- 记录：移动布局清晰；未选地点时按钮明确显示“先选择地点，再交给 AI 整理 →”，选地点后即时变为“交给 AI 整理 →”。
- AI 确认：本轮不再出现上轮“长期整理中”；降级结果字段完整，确认保存最终进入详情。
- 相册/画廊：入口可用；完整日期分组与上传后照片布局待真实文件选择器复测。
- 找地点：QA 地点出现在结果列表，点击进入地点详情并显示时间线；未复测全部筛选组合。
- 我的：登录态显示“云端已连接”，同步时间更新；暖纸、票根、竹青三主题可切换，刷新后暖纸选择保持。
- 分享：创建分享后可显示规范 `/s/p/<slug>` 链接；“已保存/下载”提示出现；新标签页打开字段完整，未出现 `undefined`。

### Happy Path

已跑通：打开首页 → 登录 → Record → 选择 QA 地点/输入文字 → AI 确认 → 保存 → 详情 → 刷新重开 → 编辑保存 → 分享 → 保存分享图 → 新标签页打开分享链接 → Find 回顾。

未完成：删除 QA 测试记录与分享快照的最终清理，需在用户确认后继续执行界面删除。

### 建议优先级

1. 在 5173 配置并复测真实 AI/ASR 服务；
2. 真实浏览器文件选择器上传 2–3 张 QA 图片，验证“设为封面”即时跟随；
3. 在 HTTPS 环境用真实麦克风完成按住录音、转写回填和超短按提示；
4. 补齐 8 页桌面/移动截图归档及下载文件落盘、无痕分享复核。

## 数据安全

本轮只创建了带 `QA测-` 前缀的地点/记录与其分享快照；未触碰“绿野书屋”等真实数据。清理动作尚未执行。
