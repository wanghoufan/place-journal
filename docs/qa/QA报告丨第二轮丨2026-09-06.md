# QA 第二轮审查结果丨2026-09-06（桌面 Chrome + 移动仿真前置版，全程只读真实数据）

> 来源：用户转交的桌面 GPT 测试报告原文（未改一字，见「报告原文」节）。
> 测试基线：桌面 Chrome（8081 部署版）、无可控手机 Safari、未代输登录、未碰密钥/环境变量。
> 开发侧注记见文末「处置」节。

## 报告原文

P1：PASS

1. 新建「T-搬家店」记录，上传 1 张照片并填写两句感受，保存成功。
2. 进入记录编辑，将地点改为「T-搬家店改」，详情页新名称生效。
3. 新建第二条记录并挂在「T-搬家店改」下，保存成功。
4. 将两条记录分别搬到「T-搬家店第二地点」，保存成功。
5. 「T-搬家店改」被搬空后，画廊中自动消失。
6. 清理完成：画廊中 T- 记录剩余 0 条；找地点中仅剩 2 个真实地点。

证据：建档、改名、搬家、清理后的画廊与找地点截图已产生。

P2：FAIL

步骤：上传门头照片 → 点击「AI识别封面文字」→ 等待 30 秒。

识别词：无。
自动匹配：无。

原文证据：
「识别失败：三个 AI 通道都认不出（opencode: timeout）。如提示“不支持看图”，请换一个 vision 模型（如 OpenRouter 的图文模型）后重试。。可手动填写。」

草稿已直接关闭，未保存。

P3：FAIL

1. 灯箱：PASS
打开现有「dv公社」记录后，全屏预览弹窗出现；「设为封面」为显式按钮。
该记录只有 1 张照片，因此左右滑动无法实际验证。

2. 文本框：FAIL

复现步骤：
- 打开「dv公社」→ 编辑。
- 在「我的感受」和「公开推荐理由」分别粘贴恰好 300 字。
- 两个文本框均保持固定高度，并出现内部滚动条。

测量证据：
- 「我的感受」：clientHeight=68，scrollHeight=335，overflowY=auto
- 「公开推荐理由」：clientHeight=65，scrollHeight=335，overflowY=auto

已点击「取消」，真实记录未修改。

P0：待真机三行。

总结论：P1 闭环 PASS；P2 OCR 因 opencode timeout FAIL；P3 文本框内部滚动条回归 FAIL；T- 测试数据已清理干净。

## 处置（开发侧，2026-09-07）

- **P1**：有效，地点击改名/搬家/搬空清理逻辑确认可用，无需改动。
- **P2（真问题，已修→后用户决定整条下线）**：根因非“不支持看图”——当时只配了 OpenCode 单通道且单通道超时仅 6s，而 GLM vision 实测需 8–17s（直调实证：看图可用、中文识别准确、JSON 合同合规）。曾放宽至 Docker 55s 后，用户决定取消 AI 识图（Vercel 15s 上限装不下、等待体验差），`api/ocr.ts` 及全链路已在 `b227ddc` 删除。
- **P3-2（测到的是旧包）**：自动撑高修复当时尚未提交上线；`useAutoGrow` 已覆盖记录页正文/详情编辑两框，随 `16f3068` 上线。待按原步骤重跑确认。
- **P0**：仍待真机三行；同步侧已上线锁超时/毒丸停放/推拉解耦/结果可视化（`c3863ae`/`573a75c`/`0b8607d`）。
