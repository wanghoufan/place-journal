- 2026-09-18｜根因：`V1_TABLES_DDL`（`mobile/src/db/schema.ts`）定版后**原地增列**（`entries.cover_media_id`、`places/entries.sync_error`、`summary`、`note_public`、`media.remote_path/remote_thumb_path/demo_uri`、`share_items.cover_media_id/cover_uri`）却未递增 migration 版本号，老库 `CREATE TABLE IF NOT EXISTS` 命中已存在表不补列 → 真机 Meine 页 `no such column` 白屏；防再犯：**改 DDL 必须同时新增 migration 版本**（迁移 v3 已按 `EXPECTED_COLUMNS` 批量补齐，`migrations.test.ts` 的新库全等校验兜底后续漏补）。
- 2026-09-18｜REWORK6 毒账本（账本已标 v1/v2/v3 但设备列仍缺、版本门控跳过补列）：列补齐从「版本门控」改为「无条件对账」——`initializeDatabase` 每次启动除跑未应用迁移外，恒跑 `reconcileExpectedColumns`（全表全列 `PRAGMA` 对账，缺列即 ALTER，幂等、不写账本版本号、只打 debug 行数）；V1/V2/V3 版本链保留不动（v3 复用同一对账函数）。
