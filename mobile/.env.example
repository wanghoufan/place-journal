# 复制为 .env（或 .env.local）后填入真实值；真实 .env 不入 Git。
# 只允许 EXPO_PUBLIC_* ：这些值会打进客户端包，禁止填任何 Secret。
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
EXPO_PUBLIC_WEB_BASE_URL=https://place-journal-xi.vercel.app
# AI 整理服务端基地址（与 Web 共用的 /api/ai-organize，Key 只在服务端，这里只填地址）。
# 例：https://place-journal-xi.vercel.app ；留空＝本机不发网，AI 整理走本地占位（mock）。
# 注意：EXPO_PUBLIC_* 在打包时内联，改完必须重新启动/重建才生效。
EXPO_PUBLIC_AI_API_BASE=
